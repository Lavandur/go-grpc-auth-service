app:
  auth:
    refresh-life-time: 20
    access-life-time: 2
  db:
    postgresql:
      host: "localhost"
      port: "5432"
      database: "auth"
      user: "auth"
      password: "auth"
      sslmode: disable
  log-level: "debug"
  default-role: "USER"